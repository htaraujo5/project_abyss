PORT=3344
ABYSS_SANDBOX=vfs
# Persistência de usuários/saves (Docker usa /app/apps/data no volume abyss_data)
# ABYSS_DATA_DIR=./apps/data
# Optional future (Postgres ainda não guarda auth/saves):
# DATABASE_URL=postgres://abyss:abyss@localhost:48291/abyss
# REDIS_URL=redis://localhost:49173
